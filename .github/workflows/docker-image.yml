name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build and Push the Docker image
      env:
        DOCKER_USER: ${{ secrets.DOCKER_TOKEN }}
        DOCKER_TOKEN: ${{ secrets.DOCKER_TOKEN }}
      run: |
        docker build . --file Dockerfile --tag erromu/devsu-demo:0.1
        echo "This message should be logged out"
        echo "<<<< ===== >>>>"
        echo $DOCKER_USER
        docker login  --username $DOCKER_USER --password $DOCKER_TOKEN
        docker push erromu/devsu-demo:0.1
